from cheqed.core import prover
from cheqed.core.sequent import Sequent

#def test_prover():
#    g = Sequent([], [prover.parse(r'(not (not a))')])
#    p = prover.assumption().normalize(g)
#    p = p.replace(p.assumptions()[0], prover.right_negation()).normalize(g)
#    p = p.replace(p.assumptions()[0], prover.left_negation()).normalize(g)
